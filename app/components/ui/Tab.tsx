import React from 'react'

interface TabProps{
    Selected : string,
    tabText : string,
    icon : React.ReactNode
}

const Tab:React.FC<TabProps> = ({Selected}) => {
  return (
    <div></div>
  )
}

export default Tab